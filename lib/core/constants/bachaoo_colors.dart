import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const primaryColor = Color(0xFF006B2E);
  static const Color secondaryColor = Color(0xFFD9A647);

  // appbackground
  static const Color appBackroundColor = Color(0xFFF5F3EF);
  static const Color surfaceColor = Color(0xFFFFFFFF);

  // Slightly tinted surface
  static const Color surfaceVariant = Color(0xFFF9F8F5);
  // Disabled / subtle background
  static const Color disabledBackground = Color(0xFFE6E3DD);

  // text colors
  static const Color textPrimary = Color(0xFF1A221A);
  static const Color textSecondary = Color(0xFF4D5A50);

  // Placeholder / hint text
  static const Color textHint = Color(0xFF8C968F);

  // Very subtle text
  static const Color textMuted = Color(0xFF9CA49E);

  // Text on primary green
  static const Color textOnPrimary = Color(0xFFFFFFFF);

  // Text on secondary gold
  static const Color textOnSecondary = Color(0xFF1A221A);

  // Text on dark backgrounds
  static const Color textOnDark = Color(0xFFFFFFFF);

  // icons colors

  // Main icons
  static const Color iconPrimary = Color(0xFF006B2E);

  // Normal secondary icons
  static const Color iconSecondary = Color(0xFF4D5A50);

  // Muted icons
  static const Color iconMuted = Color(0xFF8C968F);

  // Icons on primary background
  static const Color iconOnPrimary = Color(0xFFFFFFFF);

  // Gold icons / highlights
  static const Color iconSecondaryColor = Color(0xFFD9A647);

  // border colors

  // Normal borders
  static const Color borderColor = Color(0xFFE6E3DD);

  // Stronger border
  static const Color borderStrong = Color(0xFFD8D4CC);

  // Primary focused border
  static const Color borderFocused = Color(0xFF006B2E);

  // Gold border
  static const Color borderSecondary = Color(0xFFE8C979);

  // input colors

  static const Color inputBackground = Color(0xFFFFFFFF);

  static const Color inputBorder = Color(0xFFE6E3DD);

  static const Color inputFocusedBorder = Color(0xFF006B2E);

  static const Color inputErrorBorder = Color(0xFFC62828);

  static const Color inputHint = Color(0xFF8C968F);

  static const Color inputText = Color(0xFF1A221A);

  // success colors

  static const Color success = Color(0xFF247A45);

  static const Color successLight = Color(0xFFE5F1E8);

  static const Color successDark = Color(0xFF145A32);

  // error colors

  static const Color error = Color(0xFFC62828);

  static const Color errorLight = Color(0xFFFDEAEA);

  static const Color errorDark = Color(0xFF9B1C1C);

  // warning colors

  static const Color warning = Color(0xFFD9A647);

  static const Color warningLight = Color(0xFFFAF2DE);

  static const Color warningDark = Color(0xFF9A6A12);

  // info colors

  static const Color info = Color(0xFF3578A8);

  static const Color infoLight = Color(0xFFE8F1F7);

  // divider colors

  static const Color divider = Color(0xFFE6E3DD);

  // overlay / shadow colors

  static const Color overlay = Color(0x33000000);

  static const Color shadow = Color(0x14000000);

  // common colors

  static const Color white = Color(0xFFFFFFFF);

  static const Color black = Color(0xFF000000);

  static const Color transparent = Colors.transparent;

  // icon color
  // Bell Icon Main Outline
  static const Color iconPrimaryColor = Color(0xFF4A4A4A);
  // Border — the soft gray outline around each field
  // static const Color inputBorder = Color(0xFFD8D8D3);

  // // Hint / placeholder text
  // static const Color inputHint = Color(
  //   0xFFA0A0A0,
  // ); // Light black / soft charcoal // Dark black/charcoal

  // Notification Badge Dot
  //static const Color iconSecondaryColor = Color(0xFFB93A2B); // Muted red

  // Icon Background
  // static const Color iconBackgroundColor = Color(0xFFFFFFFF); // White
}
